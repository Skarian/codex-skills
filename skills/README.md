# Skills

Each subdirectory in `skills/` is a published installable skill.

Current skills:

- `url-to-markdown`: Local URL-to-markdown pipeline using Firecrawl self-hosted, with stdout output and idle shutdown.
